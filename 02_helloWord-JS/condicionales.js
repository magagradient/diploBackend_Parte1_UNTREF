// Las CONDICIONALES nos permiten tomar decisiones, si se ejecuta un codigo o si no, si el usuario puede entrar o no a alguna parte de nuestro codigo, etc.
// Son aquellas expresiones que nos permiten ejecutar una linea de codigo en base al resultado del analisis de una expresion.
// if - else son las condicionales mas utilizadas en js, en casi todas las situaciones y logica de codigo.

let semaforo = "Verde";

// un(signo igual) = es asignacion, dos(signos iguales juntos) == son comparacion
// toLowerCase pasa todas las letras a minuscula.

if (semaforo.toLowerCase() == 'Verde') {
    console.log('avanzar');
} else {
    console.log('detener');
}






